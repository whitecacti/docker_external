services:
  api:
    container_name: api
    build: .
    restart: unless-stopped
    volumes:
      - ./flask_api.py:/flask_api.py
    ports:
      - ${API_110_EXTERNAL_PORT}:5000
    entrypoint: /usr/bin/python /flask_api.py